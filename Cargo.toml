[package]
name = "sylt-2d"
version = "0.1.0"
edition = "2021"

[features]
log = ["dep:serde", "dep:serde_json"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }
